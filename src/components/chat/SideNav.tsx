import { Dispatch, SetStateAction } from 'react'

export default function SideNav({menuOpen, setMenuOpen} : {menuOpen : boolean, setMenuOpen: Dispatch<SetStateAction<boolean>>}) {
  return (
    <div>SideNav</div>
  )
}
